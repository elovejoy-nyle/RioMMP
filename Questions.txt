I have questions for Opto22, and want to keep track:

Q0:
I have updated the firmware on some of my testing units. 
If I make a new restoration file, where the firmware has been updated, 
will that restoration file be compatible with other Rio devices which have not had the firmware updates?
(last I checked, the firmware version of new units we are receiving is 3.5)
(when I looked at the files in the restoration zip file, I did see references to the firmware version)
<<  Regarding your questions, it’s best to perform the backup and restore on the same firmware version. 
<< It will work across versions as well, but if the backup is created on one firmware version 
<< and the restore is performed on an older one, the backup may contain features that don’t exist 
<< on the unit it’s being restored to. So, those settings will not be restored.

 
Q1:
I have been reading up on OptoMMP.  
I am curious if all the functionality of the restoration file can be written to the Rio using OptoMMP instead? 
<< You can write whatever configuration you like with MMP as well. 
<< Basically, all the backup does is read MMP data and store it in a file. 
<< The restore just reads the file and writes the data to the same MMP addresses it was read from.

Q2:
Network ports get disabled: is that normal?
<< yes, in older firmware version, that was a default.
<< If the feature is causing issues, you can increase the value or disable it (with a value of 0). 
<< You can also keep it in place and ensure the socket is used at least every 4 minutes to prevent it from timing out. 
FFFF F030 006C = TCP idle session timeout. 
FFFF F038 0020 = TCP idle session timeout in milliseconds. 0 = disable.

Q3:
Can we turn off OptoMM?
<< No, but you may block the firewall port to it. 

Q4:
For modbusTCP, do we want both ports eabled (eth0, eth1) in our firewall? 

Q5:
( here I want to define robustness of wiring config)
So if I were to enable ModbusTCP on both eth0, eth1, would this cause any problems?.
If the PLC were on Eth1, would there be erratic operation?
(in the scenario of eth1 being disabled to ModbusTCP, 
but passing the traffic of eth0 through because the port config is "switched")


Q6:
Given this restrore file: E360 restore file.zip, 
or : OptoSupport_date.jason, 
can the OptoMMP registers of the approptiate settings be provided?

